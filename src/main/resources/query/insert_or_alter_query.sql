ALTER TABLE FD_RITM_MASTER
DROP COLUMN STATUS;

ALTER TABLE FD_RITM_MASTER
    ADD COLUMN STATUS_ID BIGINT NOT NULL;

ALTER TABLE FD_RITM_MASTER
    ADD CONSTRAINT FK_RITM_STATUS
        FOREIGN KEY (STATUS_ID)
            REFERENCES FD_RITM_STATUS (STATUS_ID);